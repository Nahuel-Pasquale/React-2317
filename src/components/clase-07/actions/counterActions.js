import { TYPES } from "../types";

export const increment = () => ({ type: TYPES.INCREMENT });

export const increment_5 = () => ({ type: TYPES.INCREMENT_5 });

export const multiply = () => ({ type: TYPES.MULTIPLY });

export const decrement = () => ({ type: TYPES.DECREMENT });

export const decrement_5 = () => ({ type: TYPES.DECREMENT_5 });

export const divide = () => ({ type: TYPES.DIVIDE });

export const reset = () => ({ type: TYPES.RESET });
